import React from "react"
import { Layout } from "antd"

const { Header } = Layout

const Navigation = () => {
	return (
		<Header
			style={{
				display: "flex",
				alignItems: "center",
			}}
		></Header>
	)
}

export default Navigation
